{
  "name": "jeonpyo-cheol",
  "version": "1.0.0",
  "private": true,
  "description": "전표철 — 전장·선팅·덴트 시공 매출·지출 장부",
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "sync": "npm run build && cap sync android",
    "apk": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
